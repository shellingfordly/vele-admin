
// ! You need to clear the browser cache after the change
const setting = {

  // 切换接口时是否删除未关闭消息和通知
  closeMessageOnSwitch: true,

  // 是否取消切换接口时已经发送但没有响应的http请求
  // 如果启用，覆盖单个接口，可以设置在单独的接口吗
  removeAllHttpPending: false,
};

export default setting;
